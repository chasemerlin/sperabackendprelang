<%= form_for(@resource_page) do |f| %>
  <% if @resource_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resource_page.errors.count, "error") %> prohibited this resource_page from being saved:</h2>

      <ul>
      <% @resource_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
